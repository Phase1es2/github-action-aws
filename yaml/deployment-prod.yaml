apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-app
  namespace: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django-app
  template:
    metadata:
      labels:
        app: django-app
    spec:
      containers:
        - name: django-app
          image: hz3866/django-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000